<!DOCTYPE html>
<html>
    <head>
        <title>
            {{title}}

        </title>
        {{#each javascriptSources}}
            <script src={{this}}></script>
        {{/each}}
    </head>
    <body>
        {{{body}}}
        <h4>{{subtexttwo}}</h4>
    </body>
</html>